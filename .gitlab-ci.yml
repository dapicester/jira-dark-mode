stages:
  - package
  - test

sast:
  stage: test

include:
  - template: Security/SAST.gitlab-ci.yml

package-firefox:
  image: alpine:latest
  stage: package
  script:
    - zip -r -FS jira-dark-mode.zip * --exclude '*.git*'
